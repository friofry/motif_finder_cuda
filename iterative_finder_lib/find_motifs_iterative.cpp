#include "find_motifs_iterative.h"
//
//void get_motif_occurrences()
//{
//    // prepare sequence hashes
//    // mask found positions
//
//}
//
//void find_motifs_loop()
//{
//    while (true) {
//        get_motif_occurrences();
//
//        // exclude motifs by score threshold
//        exclude_motifs_by_score();
//
//        // find motif with highest score
//        find_most_important_motif();
//
//        // copy previous weights
//
//        // exit the loop if score is lower than threshold value
//        if (max_motif_data.score < _params.min_motif_score) {
//            break;
//        }
//
//        // exit loop if found enough motifs
//        if (_found_motifs.size() >= static_cast<uint32_t>(_params.motif_to_find)) {
//            break;
//        }
//
//        // exit loop if unique sequences count is lower than a threshold
//        if (exclude_sequence) {
//            int sequences_left = calc_unique_sequence_ids(sequences_data);
//            if (sequences_left < minimum_sequences || sequences_left == 0) {
//                break;
//            }
//        }
//    }
//}

void find_motifs_iterative()
{
//    // prepare hashes
//    auto motif_hashes = generate_motif_hashes();
//
//    // exclude by contrast sequence set
//    exclude_by_contrast_sequences(motif_hashes);
//
//    // search and exclude found sequences
//    find_motifs_loop();
//
//    // search and exclude positions
//    find_motifs_loop();
}